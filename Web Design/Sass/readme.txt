Command to Run Sass

In this directory type:

sass --watch scss:css

into the command line. This will watch the sass folder and compile valid css into the css folder

If you are cd'ing into a folder with spaces, just surround the folder name in quotes